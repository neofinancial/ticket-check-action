/* eslint-disable jest/expect-expect */
import { run } from './ticket-check-action';

// TODO: Write tests for the action
describe('ticket-check-action', () => {
  it('should run successfully', async () => {
    await run();
  });
});
